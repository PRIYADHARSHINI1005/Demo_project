Copy this prompt and attach your reference image together with your existing component files.

I already have two components on my React customer banking dashboard:

1. Upcoming Repayment table
2. Savings Account card

Redesign only these two existing components so that their structure, sizing, alignment, spacing, and visual pattern closely match the attached reference image.

Do not redesign the remaining dashboard.

OVERALL LAYOUT

Match the reference-image arrangement:

* Place the Upcoming Repayment table on the left.
* Place the Savings Account card on the right.
* Display both sections in the same horizontal row on desktop.
* The table section should occupy approximately 68–72% of the available width.
* The savings-card section should occupy approximately 28–32%.
* Maintain approximately 24–30px of space between them.
* Align both sections at the top.
* Neither component should overlap the other.
* Keep the overall layout clean, spacious, and suitable for a professional banking dashboard.

Use a layout similar to:

[ Upcoming Repayment table — wide ] [ Savings Account — narrow ]

UPCOMING REPAYMENT SECTION

Recreate the lightweight table structure visible on the left side of the reference image.

* Display the heading “Upcoming Repayments” above the table.
* Keep the heading and any “View All” action aligned on the same line.
* Position “View All” at the top-right of the table section.
* Do not place the table inside a heavy, box-like card.
* Use either no outer border or only a very subtle light-gray border.
* Use a white background.
* Avoid a strong shadow.
* Keep the table spacious and lightweight.
* Use sufficient horizontal and vertical spacing between columns and rows.
* Use subtle horizontal dividers between rows.
* Avoid strong vertical column borders.
* Keep the table header small, muted, and professional.
* Keep important values slightly darker and bolder.

Use my existing dynamic repayment data. Do not hard-code new repayment records.

Preserve my current fields. If my component already contains equivalent fields, arrange them in this order:

* Loan/Application information
* Loan ID
* Due Date
* Installment Amount
* Repayment Frequency or remaining time, if already available
* Action

Each row should follow the clean pattern of the reference table:

* Show the loan name or type clearly.
* Show its ID using smaller secondary text if appropriate.
* Display dates and currency values clearly.
* Use Indian currency formatting such as ₹4,250.
* Add a compact outlined “Pay Now” or existing repayment-action button at the end.
* The action button must not be oversized.
* Use rounded corners of approximately 18–20px for the outlined button.
* Use SCB-inspired blue or teal for the action outline and text.
* Add a subtle hover state.
* Maintain consistent row heights.
* Do not add unnecessary icons, badges, gradients, or decorations.

SAVINGS ACCOUNT CARD

Recreate the bank-card structure shown on the right side of the reference image, but do not use a completely blue background.

The Savings Account card must:

* Have the proportions and rounded rectangular shape of a physical debit or account card.
* Use a white background.
* Use a subtle light-gray border.
* Use a very soft shadow.
* Use approximately 16–20px border radius.
* Use SCB-inspired blue, teal, and green only as accents.
* Do not use a full blue fill.
* Do not use the Standard Chartered logo.
* Do not copy proprietary branding.

Preserve the same visual arrangement as the reference card:

* Place the card title or account type near the upper-left.
* Place the current or available balance prominently below it.
* Place a small card-chip-style visual near the upper-right.
* Display the account holder’s name in the middle or lower-left.
* Display a masked account number near the bottom, for example:
    “•••• •••• •••• 1234”
* Place an additional small decorative card indicator near the bottom-right only if needed.
* Use a subtle SCB blue, teal, or green curved decorative pattern in one corner.
* Keep the decoration extremely light so that the card remains mostly white.
* Ensure that all text has strong contrast and remains readable.

The white card can use:

* Dark navy for important text
* Muted gray for labels
* Blue for the chip or small accent
* Teal and green for subtle decorative lines or shapes
* A very light blue/teal background tint only in small areas

Do not add:

* A completely blue background
* Strong gradients
* Bright multicoloured decoration
* Heavy shadows
* Oversized text
* Unnecessary buttons
* Fake credit-card information that is not present in my existing component

COLOUR PALETTE

Use this SCB-inspired palette consistently:

* Blue: #0473EA
* Teal: #00A6A6
* Green: #38A949
* Dark navy text: #0B1F3A
* Secondary text: #667085
* Light border: #E4E7EC
* Dashboard background: #F7F9FC
* Card background: #FFFFFF
* Very light blue accent: #EAF4FF
* Very light teal accent: #E7F8F7
* Very light green accent: #EAF7EC

RESPONSIVE BEHAVIOUR

* Large desktop: table and savings card in one horizontal row.
* Tablet: use approximately 60% for the table and 40% for the card, if enough space exists.
* Small tablet/mobile: stack the components vertically.
* When stacked, show the Upcoming Repayment table first and the Savings Account card below it.
* Allow the table to scroll horizontally on small screens instead of compressing or hiding columns.
* Ensure the account card does not stretch excessively on large screens.
* Give the savings card a sensible maximum width.
* Do not use fixed positioning.

IMPLEMENTATION REQUIREMENTS

* Use React JSX and regular CSS only.
* Do not use Tailwind CSS.
* Do not install additional libraries.
* Modify my existing Upcoming Repayment and Savings Account components.
* Do not create duplicate versions of these components.
* Keep all current props, dynamic data, API integrations, state, event handlers, and button functionality unchanged.
* Do not hard-code the values visible in the reference image.
* Use my existing data property names.
* Do not change the surrounding dashboard functionality.
* Avoid inline CSS.
* Use component-specific CSS class names so the styles do not affect other tables or cards.
* Remove or override old styles that conflict with the new layout.
* Do not modify unrelated files.

First inspect the JSX and CSS that I provide. Then give me:

1. The complete updated Upcoming Repayment JSX
2. The complete updated Savings Account card JSX
3. The complete updated CSS
4. The exact parent Dashboard JSX needed to align both components
5. A clear list of which existing code sections I should replace

The final output must closely match the attached reference image’s structure, spacing, proportions, and alignment. The only major visual difference should be that the Savings Account card has a white background with SCB blue, teal, and green accents instead of a completely blue background.
